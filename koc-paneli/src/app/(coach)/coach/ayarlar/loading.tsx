import { PageSkeleton } from '@/components/shared/page-skeleton'

export default function CoachSettingsLoading() {
  return <PageSkeleton titleWidth="w-36" subtitleWidth="w-52" cards={2} rows={3} />
}
